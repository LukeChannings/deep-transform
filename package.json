{
  "name": "transform-tools",
  "version": "1.0.0",
  "description": "For quickly reshaping data",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build:esm": "esbuild --platform=neutral --format=esm ./src/index.ts --bundle --sourcemap --outdir=dist",
    "build:cjs": "esbuild --platform=neutral --format=cjs ./src/index.ts --bundle --sourcemap --outdir=dist"
  },
  "keywords": [],
  "author": "Luke Channings",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-env": "^7.15.8",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.2",
    "esbuild": "^0.13.4",
    "jest": "^27.2.4",
    "typescript": "^4.4.3"
  }
}
